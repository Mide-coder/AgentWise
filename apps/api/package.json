{
  "name": "@agentwise/api",
  "version": "0.1.0",
  "description": "AgentWise REST API — Goal Engine backend",
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "dev": "node --import tsx/esm src/server.ts",
    "dev:watch": "node --watch --import tsx/esm src/server.ts",
    "build": "tsc --project tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest run",
    "db:persistence-check": "node --import tsx/esm src/scripts/persistence-check.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agentwise/sdk": "workspace:*",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "xrpl": "^3.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "tsx": "^4.15.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
